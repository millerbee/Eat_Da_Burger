<tr class="table-row">

	<td class="table-first-col"> {{burger_name}}</td>
	<td><button class="change-devour" style="width: 100px; margin-top: 10px; margin-left: 5px;" data-id="{{id}}" data-newdevour="{{devoured}}">{{#if devoured}}DEVOURED!{{else}}READY!{{/if}}</button></td>
	
</tr>

{{!-- 
<div class="burger-div">
	<div class="row">
		<div class="col-md-7 col-7">
			{{burger_name}}
		</div>
		<div class="col-md-3 col-3">
			<button class="change-devour btn btn-outline-secondary rounded-0" data-id="{{id}}" data-newdevour="{{devoured}}">
				{{#if devoured}}Devour It!{{else}}Reorder it!{{/if}}
			</button>
		</div>
	</div>
</div> --}}